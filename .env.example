# ============================================================
# ヒビオル (hibioru) 環境変数テンプレート
# ============================================================
# このファイルを .env.local にコピーして値を設定してください
# cp .env.example .env.local
# ============================================================

# ------------------------------------------------------------
# Supabase設定
# ------------------------------------------------------------
# Supabase Dashboard または supabase start で確認

# Supabase URL
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Supabase Anonymous Key（公開用、RLSで保護される）
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key（サーバーサイドのみ、RLSをバイパス）
# 注意: クライアントサイドで使用しないこと
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ------------------------------------------------------------
# VAPID鍵設定（Web Push通知用）
# ------------------------------------------------------------
# 鍵の生成: pnpm vapid:generate
# 参考: https://datatracker.ietf.org/doc/html/rfc8292

# VAPID公開鍵（クライアントサイドで使用）
# Service Workerでのプッシュ購読登録時に使用
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key

# VAPID秘密鍵（サーバーサイドのみ）
# 注意: 絶対に公開しないこと、コミットしないこと
VAPID_PRIVATE_KEY=your-vapid-private-key

# ------------------------------------------------------------
# 認証設定（オプション）
# ------------------------------------------------------------
# Google OAuth用（本番環境で使用）
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# ------------------------------------------------------------
# 開発環境設定
# ------------------------------------------------------------
# 開発モードフラグ
# NODE_ENV=development
